Pill from kioku. Use via `window.KiokuDS.Pill` (bundle loaded from the root `_ds_bundle.js`).

The only rounded element in the system — mono uppercase tag/filter chip.

## Props

```ts
interface PillProps {
children: React.ReactNode; active?: boolean; muted?: boolean; onClick?: () => void; className?: string; title?: string;
}
```
