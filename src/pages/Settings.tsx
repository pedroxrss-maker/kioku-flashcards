import { PageHeader } from '../components/PageHeader';
import { Panel } from '../components/Panel';

/** Placeholder — full settings arrive in step 9. */
export function Settings() {
  return (
    <div className="rise">
      <PageHeader title="Configurações" />
      <Panel className="p-8 text-muted">Configurações — em construção.</Panel>
    </div>
  );
}
