{
  "name": "kioku-ai-proxy",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker que faz proxy da API do Google Gemini para o Kioku.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "wrangler": "^4.20.0"
  }
}
