StatTile from kioku. Use via `window.KiokuDS.StatTile` (bundle loaded from the root `_ds_bundle.js`).

Big Archivo number + mono caption — the brand stat tile.

## Props

```ts
interface StatTileProps {
label: string; value: React.ReactNode; caption?: string; accent?: string;
}
```
