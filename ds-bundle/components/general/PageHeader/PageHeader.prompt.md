PageHeader from kioku. Use via `window.KiokuDS.PageHeader` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface PageHeaderProps {
title: string; subtitle?: string; action?: React.ReactNode;
}
```
