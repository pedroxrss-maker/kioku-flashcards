{
  "name": "kioku-delete-account",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker que exclui a própria conta do usuário autenticado via Supabase Auth Admin API.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.20.0"
  }
}
