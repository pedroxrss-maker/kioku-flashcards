Panel from kioku. Use via `window.KiokuDS.Panel` (bundle loaded from the root `_ds_bundle.js`).

Generic soft surface card. (Named `Panel` to avoid colliding with `Card`.)

## Props

```ts
interface PanelProps {
children: React.ReactNode; hoverable?: boolean; raised?: boolean; accentStrip?: string; className?: string; style?: React.CSSProperties; onClick?: () => void;
}
```
