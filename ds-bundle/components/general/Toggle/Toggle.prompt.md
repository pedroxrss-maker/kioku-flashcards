Toggle from kioku. Use via `window.KiokuDS.Toggle` (bundle loaded from the root `_ds_bundle.js`).

Brutalist on/off switch (hard edges, accent when on).

## Props

```ts
interface ToggleProps {
checked: boolean; onChange: (next: boolean) => void; label?: string; description?: string;
}
```
