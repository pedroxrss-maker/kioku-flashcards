{
  "name": "kioku-kiwify-webhook",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker que recebe os webhooks de venda da Kiwify e atualiza o plano do usuário no Supabase.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.20.0"
  }
}
